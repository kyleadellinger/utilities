FROM debian:latest

RUN apt update && apt upgrade -y && \
    apt install python3 -y && \
    apt install pipx -y && \
    apt install openssh-client oping vim -y 

RUN pipx install --include-deps ansible && \
    pipx ensurepath && pipx inject ansible argcomplete
